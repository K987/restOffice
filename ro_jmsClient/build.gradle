/*
 * This build file was auto generated by running the Gradle 'init' task
 * by 'kalmankostenszky' at '2016.12.11. 12:26' with Gradle 3.1
 *
 * This generated file contains a sample Java project to get you started.
 * For more details take a look at the Java Quickstart chapter in the Gradle
 * user guide available at https://docs.gradle.org/3.1/userguide/tutorial_java_projects.html
 */

// Apply the java plugin to add support for Java
/*
sourceSets {
    main {
        java {
            srcDir project(':ro_remoteApplication').file('src/main/java/hun/restoffice/remoteClient/domain')
        }
    }
}
*/

jar {
    manifest {
        attributes 'Main-Class': 'hun.restoffice.jmsClient.Main'
    }

    from {

        configurations.runtime.asFileTree.files.collect {
            it.isDirectory() ? it : zipTree(it)
        }

        configurations.compile.collect {
            it.isDirectory() ? it : zipTree(it)
        }
    }
    
    exclude 'META-INF/*.RSA', 'META-INF/*.SF','META-INF/*.DSA'

}

repositories{
	mavenCentral()
}

// In this section you declare the dependencies for your production and test code
dependencies {
	compile project(':ro_remoteApplication')
	compile group: 'org.jboss.as', name: 'jboss-as-jms-client-bom', version: '7.2.0.Final'
}
